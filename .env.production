# Loaded by `next build` (NODE_ENV=production) ahead of .env, so production
# builds bake the real domain into the client bundle (the Payload admin calls
# this URL directly). No secrets belong in this file; it is committed.
NEXT_PUBLIC_SERVER_URL=https://verboten.co.za
